# Core runtime configuration
APP_ENV=dev
NODE_ENV=development
PORT=3000
COOKIE_SECRET=replace-with-a-long-random-string

# Optional infrastructure tweaks
TRUST_PROXY_SETTING=
EMAIL_DEV_MAILBOX=true

# Superadmin credentials for first-time login
SUPERADMIN_USERNAME=admin
SUPERADMIN_PASSWORD=admin

# Database connection (use DATABASE_URL or individual settings)
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vendure
DB_USERNAME=vendure
DB_PASSWORD=vendure
DB_SCHEMA=public
DB_SSL=false
DB_POOL_MAX=5
DB_CONNECTION_TIMEOUT_MS=60000
DB_IDLE_TIMEOUT_MS=30000

# Asset storage (Cloudinary)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=vendure

# Email + Resend (used by EmailPlugin and test script)
RESEND_API_KEY=
FROM_EMAIL="YouniqueIndia <noreply@youniqueindia.com>"
VERIFY_EMAIL_URL=http://localhost:8080/verify
PASSWORD_RESET_URL=http://localhost:8080/password-reset
CHANGE_EMAIL_URL=http://localhost:8080/verify-email-address-change
TEST_EMAIL=you@example.com

# Vendure scripts (product dump, etc.)
SHOP_API_URL=http://localhost:3000/shop-api
SHOP_API_PAGE_SIZE=50

# Stellate GraphQL edge cache (optional but recommended in production)
STELLATE_SERVICE_NAME=
STELLATE_PURGE_API_TOKEN=
# Legacy token name still supported
STELLATE_PURGE_TOKEN=
STELLATE_DEBUG_MODE=false
STELLATE_DEV_MODE=true
STELLATE_PURGE_BUFFER_MS=2000
VITE_STELLATE_SHOP_API_URL=https://your-service.stellate.sh/
STELLATE_SHOP_API_URL=
